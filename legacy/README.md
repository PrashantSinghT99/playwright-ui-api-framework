# Legacy snapshot

This directory temporarily preserves the former `playwright-python` working tree for review. It is
not imported, collected, linted, type-checked, or executed by the modern framework.

Both original repositories are already preserved in the Git graph. See
[`docs/MIGRATION.md`](../docs/MIGRATION.md) for history commands and the proposed removal policy.
